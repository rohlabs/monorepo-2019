---
name: Button
menu: Components
---

import { Playground, PropsTable } from 'docz'
import { Button } from './'

# Button

Buttons make common actions more obvious and help users more easily perform them. Buttons use labels and sometimes icons to communicate the action that will occur when the user touches them.

### Best practices

- Group buttons logically into sets based on usage and importance.
- Ensure that button actions are clear and consistent.
- The main action of a group set can be a primary button.
- Select a single button variation and do not mix them.

## Basic usage

<Playground>
  <Button>Click me</Button>
</Playground>

## With different sizes

<Playground>
  <Button scale="small">Click me</Button>
  <Button scale="normal">Click me</Button>
  <Button scale="big">Click me</Button>
</Playground>

## With different colors

<Playground>
  <Button variant="primary">Click me</Button>
  <Button variant="secondary">Click me</Button>
  <Button variant="success">Click me</Button>
  <Button variant="cancel">Click me</Button>
  <Button variant="dark">Click me</Button>
  <Button variant="gray">Click me</Button>
</Playground>

## Outlined

<Playground>
  <Button variant="primary" outline={true}>
    Click me
  </Button>
  <Button variant="secondary" outline={true}>
    Click me
  </Button>
  <Button variant="success" outline={true}>
    Click me
  </Button>
  <Button variant="cancel" outline={true}>
    Click me
  </Button>
  <Button variant="dark" outline={true}>
    Click me
  </Button>
  <Button variant="gray" outline={true} borderRadius={100} bg="magenta">
    Klik aku bang
  </Button>
</Playground>

## Properties

<PropsTable of={Button} />
